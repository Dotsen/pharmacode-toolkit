# Examples

Images in `generated/` are produced by `tools/generate_samples.py` from fixed
parameters and a fixed seed; nothing here is a real package. `expected/` holds
the JSON the decoder produces for each image (added with the decode command).
